apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thomisis.fullname" . }}-test-connection"
  labels:
    {{- include "thomisis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thomisis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
