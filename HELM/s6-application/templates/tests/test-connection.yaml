apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s6-application.fullname" . }}-test-connection"
  labels:
    {{- include "s6-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "s6-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
